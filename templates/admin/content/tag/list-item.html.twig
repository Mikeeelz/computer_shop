<tr>
  <td>{{ item.id }}</td>
  <td>{{ item.title }}</td>
  <td>{{ item.slug }}</td>
  <td>
    <a href="{{ path('app.admin.content.tag.edit', {tag: item.id}) }}" class="btn btn-sm btn-default edit_link">
      <i class="fa fa-pencil" aria-hidden="true"></i>
      Редактировать
    </a>
  </td>

</tr>
